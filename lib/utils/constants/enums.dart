// LIST OF ENUMS
// Enums cannot be created inside a class

enum TextSizes { small, medium, large }

enum OrderStatus { pending, confirmed, shipped, delivered }

enum PaymentMethods { cash, creditCard, paypal, googlePay, applePay, visa, masterCard }